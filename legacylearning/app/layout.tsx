import type { Metadata, Viewport } from "next";
import { Fraunces, Nunito_Sans } from "next/font/google";
import "./globals.css";

const fraunces = Fraunces({
  subsets: ["latin"],
  variable: "--font-fraunces",
  display: "swap",
});

const nunito = Nunito_Sans({
  subsets: ["latin"],
  variable: "--font-nunito",
  display: "swap",
});

export const metadata: Metadata = {
  title: "Legacy Learning Group — See it. Coach it. Celebrate it.",
  description:
    "A working concept of the Legacy Learning Group vision loop: a tablet app that sees a child's work, coaches the next step, and celebrates the win. Sample recognition — not live OpenAI Vision.",
};

export const viewport: Viewport = {
  themeColor: "#163e5c",
  width: "device-width",
  initialScale: 1,
};

export default function RootLayout({
  children,
}: Readonly<{ children: React.ReactNode }>) {
  return (
    <html lang="en" className={`${fraunces.variable} ${nunito.variable}`}>
      <body>{children}</body>
    </html>
  );
}
