import Stub from "../components/Stub";

export default function Resources() {
  return (
    <Stub
      eyebrow="Resources"
      title="Useful things, in one place."
      body="Briefings, frameworks and downloads your clients can return to. Structured so it stays easy to maintain as the library grows."
    />
  );
}
